$('#global-products').append('<%= j render partial: 'item' ,collection: @items %>');
<% if @items.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @items %>');
<% else %>
  $('.pagination').remove();
<% end %>